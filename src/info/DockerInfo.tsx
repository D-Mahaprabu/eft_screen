import {Stack, Typography} from '@mui/material';

const DockerInfo = () => {
    return (
        <Stack>
            <Typography>
            DockerInfo
            </Typography>
        </Stack>
    )
}

export default DockerInfo;